spring:
  datasource:
    url: jdbc:postgresql://algashop-postgres:5432/ordering
  jpa:
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/testdata
    url: jdbc:postgresql://algashop-postgres:5433/ordering

algashop:
  integrations:
    payment:
      fastpay:
        hostname: http://fastpay:9995